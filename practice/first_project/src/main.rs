fn main() {
    // v0.1.0
    println!("Hello, world! This is a test. Now im testing the cargo run command >:3");

    // v0.1.1
    let mut x = 4;
    println!("x = {}", x);
    x = 5;
    println!("x now = {}", x)
}
